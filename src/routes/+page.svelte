<!-- <script>
	let data;

	async function loadData() {
		const response = await fetch('http://localhost:3000'); // Adjust path based on your route definition
		data = await response.json();
	}

	loadData();
</script>

{#if data}
	<p>Name: {data.name}</p>
	<p>Email: {data.email}</p>
	<p>Count: {data.count}</p>
	<p>Timestamp: {data.timestamp}</p>
{/if} -->
